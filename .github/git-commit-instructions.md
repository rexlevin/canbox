请使用中文生成 commit message。

格式要求：

`<type>`：<中文描述内容>

规则：

1. type 必须是以下之一：feat, fix, docs, style, refactor, test, chore, build, ci, revert, perf, merge, release
2. 描述必须使用中文，简洁明了，不超过 50 个汉字
3. commit message 应为单行，除非必要

示例：

feat：订单推送增加携号转网授权码相关字段

fix：修复携号转网授权码校验逻辑异常

refactor：重构购买页面控制器代码结构
